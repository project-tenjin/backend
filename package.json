{
  "scripts": {
    "test": "jasmine-browser-runner runSpecs --config=src/test/javascripts/jasmine-browser.json"
  },
  "devDependencies": {
    "jasmine-browser-runner": "^1.3.0",
    "jasmine-core": "^4.5.0"
  }
}
